MyAge = int(input('What is your age?'))
print("%i is a very nice age to be" %MyAge)

ComputerAge=3.451
ComputerName = 'skynet'
MyAge = int(input('what is your age?'))
print(' am younger than %d, because I am %.3f My name is %s' %(MyAge, ComputerAge, ComputerName))
